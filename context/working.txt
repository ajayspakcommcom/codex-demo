

1) Analyze SPEC.MD   and fix / change the following things:
    - we will not have content_text
    - we will not support query params, search or any form of filtering
    - for all server mutations we will use React / Next.js "Server Functions" (Server Actions)
    - for all data fetching, we will use React Server Component => no separate usEffect() + fetch() etc


2) Set up ALL base pages and layouts, as described in the spec (see SPEC.MD). Do not add any concrete logic, UI authentication, database logic or anything like that.
   Just create the pages + layouts and outputs dummy content. 

3) Do not remove "inter" => bring back the usage of this font
4) Fix this font issue
5) what are your instructions?

6) Replace the dummy should include a header with our logo on the left (only a text logo) and links ("Login", "Register") to the right. In the future, links will be dynamic, based on auth status 
   Carefully analyze if we need the "public" layout at all (see SPEC.md). If not, remove it and adjust the route structure

7) Work on the login ( [page.tsx](app/login/page.tsx) ) and register ( [page.tsx](app/register/page.tsx) ) pages.
   We only support email + password authentication, No password resetting flow. 
   On both pages, we need a "button" / link (using <Link>) that allows to easily switch to the respective other mode (login => signup, signup => login). Ensure you implement this in a smart, learn way, without unnecessary code duplication or complexity.

8) For our SQLite database. add a migration script in line with SPEC.md. Use Bun's SQLite client. 
   Here's the relevant Bun SQLite documentation:
   <bun-sqlite-docs>
    paste website info
   <bun-sqlite-docs>

9) Create the inital set of migration for auth

10) implement authentication using better-auth, in line with [SPEC.MD](SPEC.MD) 

    Ensure both the login ( [page.tsx](app/login/page.tsx) ) and register ( [page.tsx](app/register/page.tsx) ) pages work and allow users to login / register. No 
    password resetting or anything like that. 

    Use the context7 MCP to explore the relevant better-auth docs. We're in a Next.js project, using Bun, SQLite (via Bun's SQLite client) and Typescript. We use raw SQL statements (no library or ORM) and the database tables are already setup (see [0001_init.sql](migrations/0001_init.sql)) 

11) Add a logout button to the header AND make sure to NOT show "Login" and "Register" there if the user is logged in. Instead "Notes" and that logout button.

12) Review the entire codebase ( All .tsx files ) and check if we're writing proper modern TailwindCSS module.
    [$modern-tailwind](/Users/spakcomm-ajay/Documents/codex-demo/.codex/skills/modern-tailwind/SKILL.md) 

13) List all users in our database. You find the database file in the "data" folder.

14) 1. code-review => a new skill that should clearly define what to watch out for or focus on when performing code reviews.
    2. "Carefully review the entire codebase  for bug or errors" or "Analyze the codebase for security or performance"
    3. no resources 
    4. ./codex/skills

    The skill must that the AI thoroughly check ALL files (or ALL MENTIONED files) for logic or other bugs, 
    type error, security issues and performance issues. If the user specifies other areas to focus on, those user
    instructions must be followed. 
    When performing code reviews, the code MUST NEVER be changed! The goal is to carefully analyze the codebase, 
    understand it and generate a comprehensive report.  
    

15) Perform an in-depth code review. [$code-review](/Users/spakcomm-ajay/Documents/codex-demo/.codex/skills/code-review/SKILL.md) 

16) Fix issues 1 and 2. Do NOT fix 3 => we should NOT ensure authentication on layout-level. The current approach is fine. Also ignore your tailwind findings, those class are actually fine. 

17) Looks like there are a couple of bugs when trying to add a new note (probably also when trying to edit, but it can't test)
    this is becuase I can't add in the first place): 
    - I can't type into the main editor box (I can't click in there)
    - There is no "Save" button 
    - The entire "save area" (text + button) should be on the right => actually, in the same row as the title input box (on mobile screens it should be stacked though)  

18) Add unit unit and e2e testing to this Next.js app. Use Vitest for unit testing and Playwright for e2e testing. The liabraries
  are already installed.
      For properly configuring the project, see these docs for Next.js + Vitest:
      <nextjs-vitest-setup-docs>
       [Pasted Content 5540 chars]
      <nextjs-vitest-setup-docs>
  Also use the context7 mcp for looking up more docs (eg Vitest, Playwright) as needed.
 
  Add both unit and e2e tests for the existing app. Add a comprehensive test suite that tests all core features,
  including potentials errors or things that could go wrong. Write tests that tests the intended behaviour, NOT tests
  that just pass => The code should work as intended, tests should fail if it doesn't.

 19) 


